/**
 * Represents the response from the updateProjectAction.
 */
export type UpdateProjectResponse = {
  /** Indicates whether the update operation was successful. */
  success: boolean;
  /** A message describing the result of the operation. */
  message: string;
};
