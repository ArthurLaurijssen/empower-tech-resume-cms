/**
 * Represents the response from the updateExperienceAction.
 */
export type UpdateExperienceResponse = {
  /** Indicates whether the update operation was successful. */
  success: boolean;
  /** A message describing the result of the operation. */
  message: string;
};
