/**
 * Represents the response from the deleteSkillAction.
 */
export type DeleteSkillResponse = {
  /** Indicates whether the skill deletion operation was successful. */
  success: boolean;
  /** A message describing the result of the operation. */
  message: string;
};
