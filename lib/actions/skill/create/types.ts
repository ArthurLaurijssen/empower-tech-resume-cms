/**
 * Represents the response from the addSkillAction.
 */
export type CreateSkillResponse = {
  /** Indicates whether the skill addition operation was successful. */
  success: boolean;
  /** A message describing the result of the operation. */
  message: string;
};
