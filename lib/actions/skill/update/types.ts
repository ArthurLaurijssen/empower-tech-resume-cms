/**
 * Represents the response from the updateSkillAction.
 */
export type UpdateSkillResponse = {
  /** Indicates whether the skill update operation was successful. */
  success: boolean;
  /** A message describing the result of the operation. */
  message: string;
};
