/**
 * Interface representing the data returned when creating a new project.
 */
export interface CreateProjectData {
  /** The unique identifier of the newly created project */
  projectId: string;
}
