/**
 * Properties for Add Social Media Link Modal Button
 *
 * Defines the identifier required to associate a new
 * social media link with a specific developer's profile
 */
export interface AddSocialMediaLinkModalButtonProps {
  /**
   * Unique identifier for the developer
   * Ensures the new social media link is precisely
   * linked to the correct profile
   */
  developerId: string;
}
