/**
 * Properties for the DeleteDeveloperButton component.
 */
export interface DeleteDeveloperButtonProps {
  /** The unique ID of the developer to be deleted. */
  developerId: string;
  /** The name of the developer, used for accessible context in the UI. */
  developerName: string;
}
